# Posit Connect runs `npm ci` inside the bundle and cannot reach registry.npmjs.org.
# The Artifactory auth token is configured server-side by the Connect admin; do not add one here.
registry=https://artifactory.worldbank.org/artifactory/api/npm/itsdt-npm-virtual/
